(function (angular) {
  "use strict";

  angular.module('myApp').component('auth', {
    templateUrl: '/auth/views/auth.tpl.html',
    controller: [
      '$scope', 
      function ($scope) {

      }
    ]
  });

}(window.angular));
